# Worklog Codex

- Inspeccion focalizada del repo: `package.json`, lista de `.env`, remotos git y estado de rama.
- VPS configurada: Ubuntu 24.04.4, usuario `deploy`, SSH por llave, UFW, fail2ban, Nginx, Node 22, PM2, swap de 2 GB.
- Hardening SSH: login por contraseña desactivado; root solo por llave.
- Despliegue desde `https://github.com/AndreSuaza/app-souls.git` porque `main` local estaba adelantado frente a `origin/main` y coincidia con `upstream/main`.
- Commit desplegado: `cc31946 fix(boveda): restore product cards section`.
- `.env` copiado a `/opt/apps/app-souls/.env`; secretos no documentados.
- Punto corregido por el usuario: produccion debe usar `six2`; el `.env` local usado inicialmente apuntaba a `sixDev`.
- VPS corregida para usar `six2`; se normalizo `.env` remoto a UTF-8 sin BOM para que `DATABASE_URL` cargue bien.
- `NEXTAUTH_URL` remoto actualizado a `https://soulsinxtinction.com`.
- Cloudflare activo con nameservers `clayton` y `emma`.
- Nginx actualizado para `soulsinxtinction.com` y `www.soulsinxtinction.com`.
- Certbot/Let's Encrypt instalado; HTTPS funciona para raiz, www y `/boveda`.
- No se ejecuto migracion, seed ni escritura en base de datos.
- `npm ci` ejecuto `prisma generate`; solo genero Prisma Client.
- Build exitoso; app iniciada con PM2 y proxied por Nginx.
- PM2 quedo bajo systemd; `pm2-deploy` activo y resucita la lista guardada.
- Verificado `/` y `/boveda` con User-Agent tipo navegador; curl plano bloqueado por regla anti-bot.
